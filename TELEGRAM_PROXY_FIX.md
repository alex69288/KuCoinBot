# 🔧 Исправление: Оптимизация работы Telegram с прокси

## Проблема
После добавления поддержки прокси для KuCoin API, Telegram бот стал работать медленно, возникали ошибки соединения и таймауты.

## Причина
Telegram бот не использовал прокси-соединение, которое было настроено в .env файле. Все HTTP-запросы к Telegram API шли напрямую, но при этом использовались большие таймауты, рассчитанные на работу через прокси.

## Решение

### 1. Добавлена поддержка прокси для Telegram API
- Все HTTP-запросы в `telegram/bot.py` теперь используют `proxies=self.proxies`
- Прокси конфигурация читается из переменной окружения `PROXY_URL`
- Поддерживаются HTTP/HTTPS и SOCKS5 прокси

### 2. Адаптивные таймауты
Таймауты теперь зависят от того, используется ли прокси:

**Без прокси (прямое подключение):**
- test_connection: 10 секунд
- send_message: 8 секунд (первая попытка), 15 секунд (повторная)
- getUpdates (long polling): 10 секунд ожидания + 20 секунд HTTP таймаут
- callback/edit: 8 секунд
- Пауза между попытками: 1 секунда

**С прокси:**
- test_connection: 20 секунд
- send_message: 15 секунд (первая попытка), 25 секунд (повторная)
- getUpdates (long polling): 15 секунд ожидания + 25 секунд HTTP таймаут
- callback/edit: 15 секунд
- Пауза между попытками: 3 секунды

### 3. Улучшенная обработка ошибок
- Уменьшена пауза при ошибках слушателя с 10 до 3 секунд
- Таймауты в long polling теперь не считаются ошибкой (это нормальное поведение)

### 4. Добавлены зависимости для SOCKS5
В `requirements.txt` добавлен пакет `requests[socks]` для поддержки SOCKS5 прокси.

## Установка обновлений

```bash
# Установите обновленные зависимости (только если используете SOCKS5)
pip install -r requirements.txt
```

## Настройка прокси

В файле `.env` добавьте строку `PROXY_URL`:

```bash
# HTTP/HTTPS прокси
PROXY_URL=http://username:password@proxy_host:port

# SOCKS5 прокси
PROXY_URL=socks5://username:password@proxy_host:port

# Без авторизации
PROXY_URL=http://proxy_host:port
```

Если прокси не нужен, оставьте строку пустой или удалите её:
```bash
PROXY_URL=
```

## Результат
✅ Telegram бот работает быстро при прямом подключении (без прокси)
✅ Telegram бот стабильно работает через прокси
✅ Адаптивные таймауты минимизируют задержки
✅ Улучшенная обработка ошибок и восстановление соединения

## Измененные файлы
- `telegram/bot.py` - добавлена поддержка прокси и адаптивные таймауты
- `requirements.txt` - добавлена зависимость `requests[socks]`
