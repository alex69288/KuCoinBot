# ⚡ Quick Start - Режимы запуска

## 🚀 Быстрый выбор

### 👨‍💻 Разрабатываешь API endpoints?
```powershell
.\dev_mode.ps1
```
**Результат:** Hot reload — изменил файл → сервер перезагрузился 🔄

### 🤖 Тестируешь весь бот?
```powershell
python main_local.py
```
**Результат:** Всё работает полностью, включая торговлю

### 📝 Изменил логику бота/стратегию?
```powershell
# Нажми Ctrl+C
# Затем запусти снова:
python main_local.py
```

---

## 🎯 Таблица быстрого поиска

| Что ты меняешь? | Нужна ли перезагрузка? | Где это? | Команда |
|---|---|---|---|
| `webapp/server.py` (API endpoints) | ❌ Нет | dev_mode.ps1 | `.\dev_mode.ps1` |
| `webapp/api_compact_responses.py` | ❌ Нет | dev_mode.ps1 | `.\dev_mode.ps1` |
| `webapp/` (любые файлы) | ❌ Нет | dev_mode.ps1 | `.\dev_mode.ps1` |
| `core/bot.py` (логика бота) | ✅ Да | main_local.py | `Ctrl+C` + `python main_local.py` |
| `strategies/` (стратегия) | ✅ Да | main_local.py | `Ctrl+C` + `python main_local.py` |
| `config/` (конфигурация) | ✅ Да | main_local.py | `Ctrl+C` + `python main_local.py` |
| `telegram/bot.py` | ✅ Да | main_local.py | `Ctrl+C` + `python main_local.py` |

---

## 💡 Примеры

### Пример 1: Добавляешь новый endpoint

```python
# webapp/server.py - Добавил новый endpoint
@app.get("/api/my-new-endpoint")
async def my_new_endpoint():
    return {"status": "ok"}
```

**Действие:**
```powershell
# Запусти в режиме dev_mode
.\dev_mode.ps1

# Измени файл webapp/server.py
# Сохрани (Ctrl+S)
# Сервер автоматически перезагружается!
# Проверяй: http://localhost:8000/api/my-new-endpoint
```

### Пример 2: Меняешь формат компактного API

```python
# webapp/api_compact_responses.py
def compact_status_response(positions):
    return {
        'count': len(positions),
        'new_field': 'value'  # ← Добавил новое поле
    }
```

**Действие:**
```powershell
# Запусти в режиме dev_mode
.\dev_mode.ps1

# Измени файл webapp/api_compact_responses.py
# Сохрани (Ctrl+S)
# Сервер перезагружается в течение 1-2 сек
# Проверяй: http://localhost:8000/api/status?compact=1
```

### Пример 3: Меняешь стратегию торговли

```python
# strategies/ema_ml.py
def check_ema_strategy(self):
    # ... изменил логику
    return {"decision": "buy"}
```

**Действие:**
```powershell
# Используй ОБЫЧНЫЙ режим
python main_local.py

# Измени файл strategies/ema_ml.py
# Сохрани (Ctrl+S)
# ⚠️ Автоперезагрузки НЕ будет!
# Нажми Ctrl+C в консоли
# Запусти заново: python main_local.py
```

---

## 🎓 Как это работает?

### Dev Mode (Hot Reload)
```
Uvicorn + watch файлов в webapp/
    ↓
Файл изменился (сохранил Ctrl+S)
    ↓
Uvicorn заметил изменение
    ↓
Перезагрузил модуль за ~1 сек
    ↓
API уже работает с новым кодом ✅
```

### Main Local (Обычный режим)
```
Python запускает main_local.py
    ↓
Инициализируется бот (AdvancedTradingBot)
    ↓
Запускается Uvicorn сервер
    ↓
Бот работает 24/7
    ↓
Файл изменился? → Нужна перезагрузка вручную
```

---

## 📊 Performance

| Действие | Время |
|----------|-------|
| Hot reload (dev_mode) | 1-2 сек |
| Ручная перезагрузка (main_local) | 40-50 сек |
| Изменение API endpoint | Сразу видно в dev_mode |
| Изменение логики бота | Видно после перезагрузки |

---

## 🔍 Отладка

### Смотреть логи в реальном времени
```powershell
Get-Content logs/bot_*.log -Wait
```

### Последние 50 строк логов
```powershell
Get-Content logs/bot_*.log -Tail 50
```

### Очистить логи
```powershell
Remove-Item logs/bot_*.log -Force
```

---

## ❓ FAQ

**Q: Я запустил dev_mode.ps1, а сервер не перезагружается?**
A: Перезагрузка работает только для файлов в папке `webapp/`. Если меняешь другие файлы, нужна ручная перезагрузка.

**Q: Как узнать, что hot reload сработал?**
A: В консоли увидишь сообщение `INFO: Uvicorn running on ...` - это значит, что сервер перезагрузился.

**Q: Почему я не могу использовать dev_mode для торговли?**
A: Потому что бот инициализируется один раз при старте и не пересоздаётся при перезагрузке. Для торговли используй `main_local.py`.

**Q: Какой режим выбрать для работы?**
A: 
- Разработка API → `dev_mode.ps1`
- Тестирование бота → `main_local.py`
- Production → `main_server.py`

---

**Версия:** v0.1.14  
**Дата:** 13 ноября 2025 г.  
**Статус:** ✅ ГОТОВО
