# ⚡ WATCH MODE - Быстрый старт

## 🎯 Самый быстрый способ начать разработку

```powershell
.\watch_mode.ps1
```

**Вот и всё!** 🚀

Теперь:
1. Отредактируй любой файл
2. Нажми `Ctrl+S` (сохрани)
3. Подожди 1-2 сек
4. 🔄 Проект перезагружается автоматически

---

## 📊 Быстрое сравнение

| Нужен ли перезапуск? | Режим | Команда |
|---|---|---|
| ❌ **Нет** | Watch Mode ⭐ | `.\watch_mode.ps1` |
| ✅ **Да** | Main Local | `python main_local.py` |
| ✅ **Да** (Ctrl+C) | Dev Mode | `.\dev_mode.ps1` |

---

## 🎓 3 типичных сценария

### 1. Ты добавляешь новый endpoint
```python
# webapp/server.py
@app.get("/api/new")
async def new_endpoint():
    return {"data": "new"}
```
**Действие:** Сохрани → Готово! (перезагружается автоматически)

### 2. Ты меняешь стратегию торговли
```python
# strategies/ema_ml.py
def check_ema_strategy(self):
    # ... изменил логику
    pass
```
**Действие:** Сохрани → Готово! (перезагружается автоматически)

### 3. Ты исправляешь конфигурацию
```python
# config/settings.py
RISK_PERCENTAGE = 5.0  # Изменил значение
```
**Действие:** Сохрани → Готово! (перезагружается автоматически)

---

## 💡 Про .env файл

Если меняешь `.env`:
```powershell
# Нажми Ctrl+C
# Затем снова запусти
.\watch_mode.ps1
```

`.env` загружается только при старте, поэтому изменения видны после перезапуска.

---

## 🛑 Остановка

```powershell
Ctrl+C
```

---

## ❓ Что если ошибка?

Если видишь ошибку типа `ModuleNotFoundError: No module named 'watchdog'`:

```powershell
pip install watchdog
```

Потом снова запусти:
```powershell
.\watch_mode.ps1
```

---

**Версия:** v0.1.15  
**Статус:** ✅ ГОТОВО

Просто запусти `.\watch_mode.ps1` и начинай разработку! 🚀
