# 📱 Руководство по Telegram приложению для KuCoin Trading Bot

## ✅ Что было сделано

Ваш бот теперь имеет **полноценную поддержку Telegram приложения** с двумя вариантами использования:

### 1. **Улучшенный Telegram бот с inline-кнопками** ✅

- Добавлены inline-кнопки для быстрого доступа к функциям
- Улучшенное меню с возможностью редактирования сообщений
- Обработка callback-запросов от inline-кнопок
- Кнопка для открытия веб-приложения

### 2. **Telegram Mini App (веб-приложение)** ✅

- Создана полноценная веб-версия интерфейса управления ботом
- Адаптивный дизайн под мобильные устройства
- Автоматическая интеграция с темой Telegram
- Готовый интерфейс для управления настройками

## 🚀 Как использовать

### Вариант 1: Telegram бот с inline-кнопками

1. Запустите бота как обычно: `python main.py`
2. Откройте бота в Telegram и отправьте `/start`
3. Вы увидите новое меню с inline-кнопками прямо в сообщении
4. Нажимайте кнопки для быстрого доступа к функциям

### Вариант 2: Telegram Mini App (веб-приложение)

#### Шаг 1: Разместите веб-приложение на сервере

Файлы находятся в папке `webapp/`:
- `index.html` - основной файл приложения
- `styles.css` - стили
- `app.js` - логика приложения

**Важно:** Telegram Mini App требует HTTPS! Используйте:
- Хостинг с HTTPS (например, GitHub Pages, Netlify, Vercel)
- Или ngrok для локальной разработки: `ngrok http 8080`

#### Шаг 2: Настройте URL в боте

Откройте файл `telegram/menus.py` и найдите строку:

```python
{'text': '🌐 Открыть приложение', 'web_app': {'url': 'https://your-domain.com/webapp'}}
```

Замените `https://your-domain.com/webapp` на ваш реальный URL.

#### Шаг 3: Подключите API (опционально)

Для полноценной работы веб-приложения нужно создать API endpoints в боте. 

**Пример с Flask:**

```python
from flask import Flask, jsonify, request
from flask_cors import CORS

app = Flask(__name__)
CORS(app)

# Глобальная ссылка на бота (нужно передать при запуске)
bot_instance = None

@app.route('/api/status')
def get_status():
    if not bot_instance:
        return jsonify({'error': 'Bot not initialized'}), 500
    
    market_data = bot_instance.exchange.get_market_data(
        bot_instance.settings.trading_pairs['active_pair']
    )
    balance = bot_instance.exchange.get_balance()
    
    return jsonify({
        'pair': bot_instance.settings.get_active_pair_name(),
        'price': market_data['current_price'] if market_data else 0,
        'position': 'Открыта' if bot_instance.position == 'long' else 'Ожидание',
        'strategy': bot_instance.settings.get_active_strategy_name(),
        'balance': {
            'usdt': balance['total_usdt'] if balance else 0,
            'btc': balance['total_btc'] if balance else 0
        },
        'stats': {
            'trades': bot_instance.metrics.total_trades,
            'winRate': bot_instance.metrics.win_rate,
            'profit': bot_instance.metrics.total_profit
        },
        'trading': bot_instance.settings.settings.get('trading_enabled', False)
    })

@app.route('/api/toggle-trading', methods=['POST'])
def toggle_trading():
    if not bot_instance:
        return jsonify({'error': 'Bot not initialized'}), 500
    
    bot_instance.settings.settings['trading_enabled'] = \
        not bot_instance.settings.settings.get('trading_enabled', False)
    bot_instance.settings.save_settings()
    
    return jsonify({
        'trading': bot_instance.settings.settings['trading_enabled']
    })

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=8080)
```

Затем обновите `webapp/app.js` для использования реальных API:

```javascript
async function loadData() {
    try {
        const response = await fetch('http://your-bot-api:8080/api/status');
        const data = await response.json();
        updateUI(data);
        updateStatus('connected');
    } catch (error) {
        console.error('Ошибка загрузки данных:', error);
        updateStatus('error');
    }
}
```

## 📋 Новые возможности

### Inline-кнопки
- Быстрый доступ к функциям без ввода команд
- Меню прямо в сообщении
- Редактирование сообщений вместо отправки новых

### Web App
- Полноценный веб-интерфейс
- Адаптивный дизайн
- Интеграция с темой Telegram
- Тактильная отдача (вибрация)

## 🔧 Настройка

### Изменение кнопок меню

Откройте `telegram/menus.py` и найдите метод `send_main_menu_inline()`:

```python
inline_keyboard = {
    'inline_keyboard': [
        [
            {'text': '📊 Статус', 'callback_data': 'status'},
            {'text': '💼 Аккаунт', 'callback_data': 'account_info'}
        ],
        # ... добавьте свои кнопки
    ]
}
```

### Добавление новых callback обработчиков

В файле `telegram/handlers.py` в методе `handle_callback()`:

```python
elif callback_data == "your_command":
    # Ваш код обработки
    pass
```

## 🌐 Развертывание веб-приложения

### Вариант 1: GitHub Pages (бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите папку `webapp/`
3. Включите GitHub Pages в настройках репозитория
4. Используйте URL: `https://your-username.github.io/repo-name/webapp/`

### Вариант 2: Netlify (бесплатно)

1. Зарегистрируйтесь на [Netlify](https://netlify.com)
2. Перетащите папку `webapp/` в Netlify
3. Получите HTTPS URL автоматически

### Вариант 3: Локальная разработка с ngrok

```bash
# Установите ngrok
# https://ngrok.com/download

# Запустите веб-сервер (например, Python)
cd webapp
python -m http.server 8080

# В другом терминале запустите ngrok
ngrok http 8080

# Используйте HTTPS URL из ngrok
```

## 📝 Примечания

- Telegram Mini App требует **HTTPS** (обязательно)
- Веб-приложение автоматически определяет тему Telegram (светлая/темная)
- Все функции бота доступны через оба интерфейса
- Inline-кнопки работают сразу, веб-приложение требует настройки

## 🐛 Решение проблем

### Inline-кнопки не работают
- Убедитесь, что токен бота правильный
- Проверьте логи на наличие ошибок
- Убедитесь, что обработчик callback правильно настроен

### Web App не открывается
- Проверьте, что URL использует HTTPS
- Убедитесь, что URL доступен публично
- Проверьте настройки в BotFather

### Данные не загружаются в Web App
- Проверьте CORS настройки на API сервере
- Убедитесь, что API endpoint доступен
- Проверьте консоль браузера на ошибки

## 🎉 Готово!

Теперь у вас есть полноценное Telegram приложение для управления торговым ботом! 

Вы можете использовать:
- ✅ Улучшенный бот с inline-кнопками (работает сразу)
- ✅ Веб-приложение через Telegram Mini App (требует настройки)

