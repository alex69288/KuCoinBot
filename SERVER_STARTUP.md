# Инструкция по запуску бота на сервере

## ✅ Текущий статус
Все зависимости установлены, бот готов к запуску!

## 📋 Команды для запуска на сервере

### 1. Подключение к серверу
```bash
ssh root@box-870236.sprintbox.ru -p 5823
```

### 2. Переход в директорию проекта
```bash
cd /home/botuser/KuCoinBot
```

### 3. Активация виртуального окружения
```bash
source venv/bin/activate
```

### 4. Проверка готовности (опционально)
Запустите комплексную проверку для убедительности:
```bash
python tests/run_all_checks.py
```

### 5. Запуск бота
```bash
python main.py
```

## 🔄 Запуск бота в фоновом режиме с автозапуском

### Вариант 1: Использование screen (простой способ)
```bash
# Создание новой сессии screen
screen -S kucoin_bot

# Запуск бота
python main.py

# Отключение от сессии: Ctrl+A, затем D
# Возврат к сессии: screen -r kucoin_bot
# Список сессий: screen -ls
```

### Вариант 2: Использование systemd (рекомендуется для production)

1. Создайте файл сервиса:
```bash
sudo nano /etc/systemd/system/kucoin-bot.service
```

2. Вставьте следующее содержимое:
```ini
[Unit]
Description=KuCoin Trading Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/home/botuser/KuCoinBot
Environment="PATH=/home/botuser/KuCoinBot/venv/bin"
ExecStart=/home/botuser/KuCoinBot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

3. Перезагрузите systemd и запустите сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable kucoin-bot
sudo systemctl start kucoin-bot
```

4. Проверьте статус:
```bash
sudo systemctl status kucoin-bot
```

5. Просмотр логов:
```bash
sudo journalctl -u kucoin-bot -f
```

### Вариант 3: Использование nohup (простой фоновый запуск)
```bash
nohup python main.py > bot.log 2>&1 &
```

Просмотр логов:
```bash
tail -f bot.log
```

## 📊 Мониторинг

### Просмотр логов бота
```bash
tail -f logs/bot.log
```

### Проверка запущенных процессов
```bash
ps aux | grep python
```

### Остановка бота
Если запущен через screen:
```bash
screen -r kucoin_bot
# Затем Ctrl+C
```

Если запущен через systemd:
```bash
sudo systemctl stop kucoin-bot
```

Если запущен через nohup:
```bash
pkill -f "python main.py"
```

## 🔧 Обновление бота

```bash
cd /home/botuser/KuCoinBot
git pull
source venv/bin/activate
pip install -r requirements.txt
# Перезапустите бота
```

## ⚠️ Важные замечания

1. **Переменные окружения**: Убедитесь, что файл `.env` существует и содержит все необходимые ключи
2. **Права доступа**: Файлы должны принадлежать пользователю `botuser`
3. **Порты**: Если используется веб-интерфейс, убедитесь, что порт 8000 открыт
4. **Логирование**: Регулярно проверяйте логи на наличие ошибок

## 📱 Telegram бот

После запуска бота вы можете управлять им через Telegram:
- Отправьте `/start` боту
- Используйте команды из меню для управления торговлей

## 🆘 Устранение неполадок

### Если бот не запускается:
1. Проверьте логи: `tail -50 logs/bot.log`
2. Проверьте переменные окружения: `python tests/test_server_config.py`
3. Проверьте подключение к API: `python tests/test_kucoin_api.py`

### Если бот падает:
1. Проверьте логи на наличие ошибок
2. Убедитесь, что баланс на бирже достаточен
3. Проверьте настройки в `bot_settings.json`

## 📞 Поддержка

При возникновении проблем проверьте:
- Документацию KuCoin API: https://www.kucoin.com/docs-new
- Логи бота
- Статус биржи: https://status.kucoin.com/
