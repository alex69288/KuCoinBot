# 📱 Telegram Web App для KuCoin Trading Bot

## 🌟 Обзор

Telegram Web App предоставляет полнофункциональный веб-интерфейс для управления торговым ботом прямо из Telegram. Это современная альтернатива командному боту с красивым интерфейсом и расширенными возможностями.

## ✨ Возможности

- 📊 **Мониторинг в реальном времени** - статус бота, баланс, открытые позиции
- 💹 **Данные рынка** - актуальная информация о ценах и изменениях
- ⚙️ **Управление настройками** - изменение параметров торговли
- 🎯 **Управление ботом** - запуск/остановка одним нажатием
- 📈 **Статистика торговли** - детальные метрики и анализ
- 🎨 **Адаптивный дизайн** - поддержка темной/светлой темы Telegram
- 🔒 **Безопасность** - проверка подлинности данных Telegram

## 📁 Структура

```
webapp/
├── __init__.py          # Инициализация модуля
├── server.py            # FastAPI сервер с REST API
└── static/
    └── index.html       # Веб-интерфейс
```

## 🚀 Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Будут установлены:
- `fastapi` - современный веб-фреймворк
- `uvicorn` - ASGI сервер для FastAPI
- `pydantic` - валидация данных

### 2. Настройка переменных окружения

Добавьте в файл `.env`:

```env
# Telegram Bot (уже должны быть)
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id

# Web App URL (замените на ваш реальный URL)
WEBAPP_URL=https://your-domain.com
```

### 3. Запуск Web App сервера

#### Вариант A: Запуск отдельно от бота

```bash
# В отдельном терминале
cd c:\Users\user\Documents\Scripts\KuCoinBotV4Copilot
python -m webapp.server
```

Сервер запустится на `http://0.0.0.0:8000`

#### Вариант B: Интеграция в основной бот

Создайте файл `main_with_webapp.py`:

```python
"""
Запуск бота с Web App
"""
import sys
import os
import threading
import uvicorn
from utils.logger import log_info

def start_webapp_server():
    """Запускает Web App сервер в отдельном потоке"""
    log_info("🌐 Запуск Web App сервера...")
    uvicorn.run(
        "webapp.server:app",
        host="0.0.0.0",
        port=8000,
        log_level="info"
    )

def main():
    """Основная функция запуска"""
    from core.bot import AdvancedTradingBot
    from webapp.server import set_trading_bot
    
    # Создаем бота
    log_info("⚡ Создание торгового бота...")
    bot = AdvancedTradingBot()
    
    # Устанавливаем бота в Web App сервер
    set_trading_bot(bot)
    
    # Запускаем Web App сервер в отдельном потоке
    webapp_thread = threading.Thread(target=start_webapp_server, daemon=True)
    webapp_thread.start()
    
    # Запускаем основной цикл бота
    log_info("🤖 Запуск основного цикла бота...")
    bot.run()

if __name__ == "__main__":
    main()
```

Запуск:
```bash
python main_with_webapp.py
```

## 🌐 Развертывание (Production)

### Вариант 1: ngrok (для тестирования)

```bash
# Установите ngrok
# Запустите туннель
ngrok http 8000
```

Скопируйте HTTPS URL (например, `https://abcd1234.ngrok.io`) и добавьте в `.env`:
```env
WEBAPP_URL=https://abcd1234.ngrok.io
```

### Вариант 2: VPS/Dedicated Server

1. **Установите на сервер**:
   ```bash
   git clone your-repo
   cd KuCoinBotV4Copilot
   pip install -r requirements.txt
   ```

2. **Настройте Nginx** (опционально):
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://127.0.0.1:8000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

3. **Настройте SSL** (обязательно для Telegram Web App):
   ```bash
   certbot --nginx -d your-domain.com
   ```

4. **Запустите как сервис** (systemd):
   ```ini
   [Unit]
   Description=KuCoin Trading Bot Web App
   After=network.target
   
   [Service]
   Type=simple
   User=your-user
   WorkingDirectory=/path/to/KuCoinBotV4Copilot
   ExecStart=/usr/bin/python3 main_with_webapp.py
   Restart=always
   
   [Install]
   WantedBy=multi-user.target
   ```

### Вариант 3: Облачные платформы

#### Heroku
```bash
# Создайте Procfile
echo "web: uvicorn webapp.server:app --host 0.0.0.0 --port $PORT" > Procfile
git push heroku main
```

#### Railway.app / Render.com
- Подключите GitHub репозиторий
- Укажите команду запуска: `uvicorn webapp.server:app --host 0.0.0.0 --port $PORT`
- Добавьте переменные окружения

## 🎯 Настройка в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Выберите "Edit Menu Button URL"
6. Введите URL: `https://your-domain.com`
7. Введите название кнопки: "Открыть приложение"

Теперь у бота появится кнопка меню для открытия Web App!

## 📝 API Endpoints

### GET `/api/health`
Проверка работоспособности сервера

### GET `/api/status`
Получить статус бота (баланс, позиции, настройки, метрики)

**Параметры:**
- `init_data` (query) - данные авторизации Telegram Web App

### GET `/api/market`
Получить данные рынка для торговой пары

**Параметры:**
- `init_data` (query) - данные авторизации
- `symbol` (query, optional) - торговая пара

### POST `/api/bot/start`
Запустить бота

**Body:**
```json
{
  "init_data": "telegram_init_data"
}
```

### POST `/api/bot/stop`
Остановить бота

### GET `/api/settings`
Получить все настройки

### POST `/api/settings`
Обновить настройки

**Body:**
```json
{
  "init_data": "telegram_init_data",
  "category": "risk_settings",
  "key": "risk_per_trade",
  "value": 2.0
}
```

### GET `/api/trades`
Получить историю сделок

**Параметры:**
- `init_data` (query)
- `limit` (query, optional) - количество записей (по умолчанию 50)

## 🔒 Безопасность

### Проверка данных Telegram

Web App автоматически проверяет подлинность данных от Telegram с помощью HMAC-SHA256. Это защищает API от несанкционированного доступа.

**Как это работает:**
1. Telegram передает `initData` при открытии Web App
2. Сервер проверяет подпись используя токен бота
3. Только валидные запросы обрабатываются

### Рекомендации:

- ✅ Используйте HTTPS в production
- ✅ Не выкладывайте токен бота в публичный репозиторий
- ✅ Ограничьте доступ к серверу (firewall)
- ✅ Регулярно обновляйте зависимости

## 🧪 Тестирование

### Локальное тестирование

1. Запустите сервер:
   ```bash
   python -m webapp.server
   ```

2. Откройте в браузере:
   ```
   http://localhost:8000
   ```

3. Проверьте API:
   ```bash
   curl http://localhost:8000/api/health
   ```

### Тестирование с Telegram

Создайте тестовый файл `tests/test_webapp.py`:

```python
import requests

API_URL = "http://localhost:8000/api"

# Тест проверки здоровья
response = requests.get(f"{API_URL}/health")
print(f"Health check: {response.json()}")

# Тест получения статуса (требует init_data)
# response = requests.get(f"{API_URL}/status?init_data=...")
```

## 📊 Мониторинг

### Логи

Все действия логируются через `utils.logger`:
- Запуск/остановка бота через Web App
- Изменение настроек
- Ошибки авторизации

### Метрики

Можно интегрировать:
- Prometheus для метрик
- Sentry для отслеживания ошибок
- ELK Stack для анализа логов

## 🐛 Troubleshooting

### Web App не открывается

1. Проверьте, что сервер запущен: `curl http://localhost:8000/api/health`
2. Убедитесь, что URL в `.env` правильный
3. Проверьте, что используется HTTPS (требование Telegram)

### Ошибка авторизации

1. Проверьте токен бота в `.env`
2. Убедитесь, что `init_data` передается корректно
3. Проверьте логи сервера

### Данные не обновляются

1. Проверьте, что бот запущен и `trading_bot` установлен
2. Проверьте интервал обновления в `index.html` (5 секунд по умолчанию)
3. Проверьте консоль браузера на ошибки JavaScript

## 📚 Дополнительные ресурсы

- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [KuCoin API Documentation](https://www.kucoin.com/docs-new)

## 🎉 Готово!

Теперь у вас есть полнофункциональный Web App для управления торговым ботом! 🚀

Для вопросов и предложений создавайте Issue в репозитории.
