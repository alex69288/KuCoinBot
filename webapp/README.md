# Telegram Mini App для KuCoin Trading Bot

Это веб-приложение для управления торговым ботом через Telegram Mini App.

## 🚀 Установка и настройка

### 1. Размещение файлов

Разместите файлы из папки `webapp` на веб-сервере с HTTPS (обязательно для Telegram Web App).

### 2. Настройка URL в боте

Обновите URL в файле `telegram/menus.py`:

```python
{'text': '🌐 Открыть приложение', 'web_app': {'url': 'https://your-domain.com/webapp'}}
```

Замените `https://your-domain.com/webapp` на ваш реальный URL.

### 3. Подключение к API бота

В файле `webapp/app.js` нужно подключить реальные API запросы к вашему боту. 

Создайте API endpoint в вашем боте (например, используя Flask или FastAPI):

```python
# Пример с Flask
from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/api/status')
def get_status():
    # Получить данные из бота
    return jsonify({
        'pair': 'BTC/USDT',
        'price': 43250.50,
        # ... другие данные
    })
```

### 4. Настройка бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Используйте команду `/setmenubutton`
3. Укажите URL вашего веб-приложения

## 📱 Функциональность

- 📊 Просмотр текущего статуса бота
- 💰 Информация о балансе
- 📈 Статистика торговли
- ⚡ Быстрое управление торговлей
- ⚙️ Настройки бота

## 🔧 Разработка

Для локальной разработки используйте ngrok или аналогичный сервис для создания HTTPS туннеля:

```bash
ngrok http 8080
```

Затем используйте полученный HTTPS URL в настройках бота.

## 📝 Примечания

- Telegram Mini App требует HTTPS
- Приложение автоматически адаптируется под тему Telegram
- Использует Telegram Web App API для интеграции

