# 🤖 KuCoin Trading Bot v0.1.16

Автоматический торговый бот для биржи **KuCoin** с интегрированным веб-приложением и поддержкой Telegram.

## 📋 Таблица содержания

- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Запуск](#-запуск)
- [Режимы разработки](#-режимы-разработки)
- [Конфигурация](#-конфигурация)
- [Структура проекта](#-структура-проекта)
- [Деплой](#-деплой)

---

## ⚡ Быстрый старт

### 1️⃣ Для локальной разработки (Watch Mode):

```powershell
.\watch_mode.ps1
```

**Что это делает:**
- Запускает бота в режиме разработки (`main_dev.py`)
- Автоматически перезагружает при изменении файлов
- Загружает переменные из `.env` файла

---

### 2️⃣ Для серверного деплоя:

Используется файл `main.py` (без загрузки `.env` файла)

---

## 📦 Установка

### Требования:
- Python 3.9+
- pip

### Шаги установки:

```bash
# 1. Клонируем репозиторий
git clone https://github.com/alex69288/KuCoinBot.git
cd KuCoinBot

# 2. Создаем виртуальное окружение
python -m venv venv

# 3. Активируем виртуальное окружение
# На Windows:
venv\Scripts\activate

# На Linux/macOS:
source venv/bin/activate

# 4. Устанавливаем зависимости
pip install -r requirements.txt

# 5. Копируем конфигурацию
cp .env.example .env

# 6. Редактируем .env с вашими данными
# KUCOIN_API_KEY=ваш_ключ
# KUCOIN_SECRET_KEY=ваш_секрет
# И т.д.
```

---

## 🚀 Запуск

### Локальная разработка (рекомендуется):

```powershell
# Watch Mode - автоматическая перезагрузка при изменениях
.\watch_mode.ps1
```

**Преимущества:**
✅ Автоматическая перезагрузка при сохранении файлов  
✅ Загружает переменные из `.env`  
✅ Идеально для разработки  

---

### Локальный запуск без watch:

```bash
python main_dev.py
```

**Когда использовать:**
- Если нужна стабильность без автопересчитывания
- Для тестирования

---

### Серверный запуск:

```bash
python main.py
```

**Отличия от `main_dev.py`:**
- ❌ НЕ загружает файл `.env`
- ✅ Использует переменные окружения системы
- ✅ Для Amvera, Railway и других хостингов

---

## 💻 Режимы разработки

### Watch Mode (Рекомендуется)

```powershell
.\watch_mode.ps1
```

**Что происходит:**
1. Запускает `main_dev.py`
2. Мониторит все файлы проекта
3. При изменении файла перезагружает бота через 2 сек

**Мониторит:** `core/`, `strategies/`, `webapp/`, `config/`, `telegram/`, `utils/`, все остальные файлы

**НЕ мониторит:** `requirements.txt`, `.env`, папки кэша

---

## ⚙️ Конфигурация

### Файл `.env`

```bash
# KuCoin API Credentials
KUCOIN_API_KEY=your_api_key
KUCOIN_SECRET_KEY=your_secret_key
KUCOIN_PASSPHRASE=your_passphrase

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# Trading Settings
SYMBOL=BTC-USDT          # Trading pair
LEVERAGE=1               # Margin leverage (1, 2, 3, 5, 10)
POSITION_SIZE=100        # USDT per position
MAX_POSITIONS=5          # Maximum open positions
STOP_LOSS_PERCENT=2      # Stop loss percentage
TAKE_PROFIT_PERCENT=5    # Take profit percentage
```

### Файл `bot_settings.json`

```json
{
  "trading_enabled": true,
  "telegram_enabled": true,
  "max_daily_loss_percent": 5,
  "max_positions": 5,
  "position_size": 100
}
```

---

## 📂 Структура проекта

```
KuCoinBot/
├── main.py              # ✅ Серверная версия (для хостинга)
├── main_dev.py          # ✅ Локальная разработка (с .env)
├── watch_mode.ps1       # ✅ Watch mode - авто-перезагрузка
│
├── core/
│   ├── bot.py           # Основная логика бота
│   ├── exchange.py      # Интеграция с KuCoin API
│   └── risk_manager.py  # Управление рисками
│
├── strategies/          # Стратегии торговли
├── telegram/            # Telegram интеграция
├── webapp/              # Web приложение (REST API)
├── config/              # Конфигурация
├── utils/               # Утилиты
├── analytics/           # Аналитика и метрики
├── tests/               # Тесты
│
├── requirements.txt     # Python зависимости
├── .env.example         # Пример переменных окружения
├── bot_settings.json    # Параметры бота
└── docs/                # Документация
```

---

## 🌐 Деплой

### Amvera Cloud

```bash
# Конфигурация автоматически берет из amvera.yml
# Запускает: python main.py
```

**Файл:** `amvera.yml`

---

### Railway

```bash
# Конфигурация автоматически берет из railway.json
# Запускает: python main.py
```

**Файл:** `railway.json`

---

### Любой другой хостинг

```bash
# Запустите команду:
python main.py

# Переменные окружения установите в панели хостинга:
# KUCOIN_API_KEY
# KUCOIN_SECRET_KEY
# KUCOIN_PASSPHRASE
# TELEGRAM_BOT_TOKEN
# TELEGRAM_CHAT_ID
```

---

## 📊 Мониторинг

### Логи

Логи находятся в папке `logs/`:

```bash
tail -f logs/bot.log          # Логи бота
tail -f logs/trading.log      # Логи торговли
tail -f logs/errors.log       # Ошибки
```

### Web Dashboard

Доступен по адресу:
```
http://localhost:5000
```

**Функции:**
- 📈 Просмотр открытых позиций
- 💰 История торговли
- ⚙️ Управление параметрами
- 📊 Статистика

---

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest tests/

# Запуск конкретного теста
pytest tests/test_bot.py -v

# С покрытием кода
pytest tests/ --cov=core --cov=strategies
```

---

## 🐛 Устранение неполадок

### Ошибка: "ModuleNotFoundError: No module named 'kucoin'"

```bash
pip install -r requirements.txt
```

### Ошибка: "API credentials not found"

Убедитесь, что `.env` файл существует и содержит:
```bash
KUCOIN_API_KEY=...
KUCOIN_SECRET_KEY=...
KUCOIN_PASSPHRASE=...
```

### Watch Mode не перезагружает

1. Проверьте, что скрипт запущен:
```powershell
Get-Process python
```

2. Убедитесь, что файлы сохраняются (Ctrl+S в редакторе)

3. Перезагрузитесь вручную (Ctrl+C и снова `.\watch_mode.ps1`)

---

## 📖 Документация

Подробная документация находится в папке `docs/`:

- `DEPLOYMENT_GUIDE.md` - Гайд по деплою
- `DEV_MODES.md` - Описание режимов разработки
- `ENVIRONMENT_SETUP.md` - Настройка окружения

---

## 📝 Версионирование

Версия формата: `[vX.Y.Z]`

- **X** - Основные изменения
- **Y** - Новые функции
- **Z** - Исправления ошибок

Текущая версия: **v0.1.16** 🎯

---

## 📄 Лицензия

MIT License - см. файл `LICENSE`

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `logs/errors.log`
2. Посмотрите документацию в `docs/`
3. Создайте Issue на GitHub

---

## 🚀 Дорожная карта

- [ ] Поддержка нескольких бирж (Binance, OKX)
- [ ] Продвинутые стратегии ML
- [ ] Мобильное приложение
- [ ] Docker контейнеризация
- [ ] Улучшенная аналитика

---

**Последнее обновление:** 13 ноября 2025 г.  
**Автор:** alex69288  
**Статус:** 🟢 Активно разрабатывается
