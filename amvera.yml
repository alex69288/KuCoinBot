# Конфигурация для Amvera Cloud - Node.js Backend# Конфигурация для Amvera Cloud - Node.js Backend

# https://docs.amvera.ru/# https://docs.amvera.ru/



meta:meta:

  environment: nodejs  environment: nodejs

  toolchain:  toolchain:

    name: npm    name: npm

    version: 20    version: 20



build:build:

  additionalCommands:  args:

    - npm run build    - cd backend && npm install

    - cd backend && npm run build

run:

  command: cd backend && node dist/index.jsrun:

  persistenceMount: /data  command: cd backend && node dist/index.js

  containerPort: 3000  persistenceMount: /data

  containerPort: 3000

# Переменные окружения (устанавливаются в Amvera UI)
# KUCOIN_API_KEY
# KUCOIN_API_SECRET
# KUCOIN_API_PASSPHRASE
# KUCOIN_TESTNET=false
# NODE_ENV=production

# Health check
healthcheck:
  path: /health
  interval: 30
  timeout: 10
  retries: 3
