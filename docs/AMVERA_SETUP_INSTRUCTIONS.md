# 🚀 Инструкция по деплою на Amvera Cloud

## Проблема
Старый проект на Amvera закешировал Python-конфигурацию и игнорирует `amvera.yml`.

## Решение: Создать проект заново

### Шаг 1: Удалить старый проект
1. Зайти на https://amvera.ru/
2. Открыть проект `kucoinbot`
3. Перейти в **Settings** (Настройки)
4. Внизу страницы нажать **"Delete Project"** (Удалить проект)
5. Подтвердить удаление

### Шаг 2: Создать новый проект
1. На главной странице Amvera нажать **"Create New Project"**
2. Выбрать **"From Git Repository"**
3. Подключить GitHub (если еще не подключен)
4. Выбрать репозиторий: `alex69288/KuCoinBot`
5. Выбрать ветку: `main`
6. **ВАЖНО**: Убедиться, что выбран тип проекта: **Node.js**
7. Нажать **"Create"**

### Шаг 3: Настроить переменные окружения
В настройках проекта добавить переменные:

```bash
KUCOIN_API_KEY=your_api_key_here
KUCOIN_API_SECRET=your_api_secret_here
KUCOIN_API_PASSPHRASE=your_passphrase_here
KUCOIN_TESTNET=false
NODE_ENV=production
PORT=3000
```

Опционально (если используете Telegram):
```bash
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### Шаг 4: Проверить конфигурацию
Amvera должна автоматически определить:
- **Build Command**: `npm ci && npm run build` (из `amvera.yml`)
- **Start Command**: `node dist/index.js` (из `amvera.yml`)
- **Port**: 3000

### Шаг 5: Запустить деплой
1. Нажать **"Deploy"**
2. Следить за логами сборки
3. Должны увидеть:
   ```
   ✓ npm install completed
   ✓ TypeScript build successful
   ✓ dist/index.js exists
   ```

### Шаг 6: Проверить работу
После успешного деплоя:
1. Открыть URL приложения (предоставит Amvera)
2. Проверить health check: `https://your-app.amvera.io/health`
3. Должен вернуть статус `200 OK`

## Структура проекта (текущая)

```
/
├── src/              # TypeScript исходники
├── dist/             # Скомпилированный JS (создается при сборке)
├── package.json      # Node.js зависимости и скрипты
├── tsconfig.json     # TypeScript конфигурация
├── amvera.yml        # Конфигурация для Amvera
├── Procfile          # Fallback для автоопределения
└── runtime.txt       # Указывает на Node.js
```

## Важные файлы

### amvera.yml
```yaml
meta:
  environment: nodejs
  toolchain:
    name: npm
    version: 20

build:
  args:
    - npm ci
    - npm run build

run:
  command: node dist/index.js
  containerPort: 3000
```

### package.json (скрипты)
```json
{
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  }
}
```

## Возможные проблемы и решения

### Проблема 1: "Cannot find module '/app/backend/dist/index.js'"
**Причина**: Старая кешированная конфигурация
**Решение**: Удалить проект и создать заново (Шаги 1-2)

### Проблема 2: Python ошибки (main.py)
**Причина**: Amvera определила проект как Python
**Решение**: Убедиться что выбран Node.js при создании проекта

### Проблема 3: "tsc: command not found"
**Причина**: TypeScript не установлен
**Решение**: Проверить что `typescript` есть в `devDependencies` в `package.json`

### Проблема 4: Build успешен, но приложение не запускается
**Причина**: Ошибки в коде или отсутствие переменных окружения
**Решение**: Проверить Application Logs в Amvera

## Логи для отладки

### Build Logs (логи сборки)
Показывают процесс установки зависимостей и компиляции TypeScript

### Application Logs (логи приложения)
Показывают вывод приложения после запуска

## Контакты для поддержки
- Документация Amvera: https://docs.amvera.ru/
- GitHub Issues: https://github.com/alex69288/KuCoinBot/issues

---

**Дата создания**: 2025-11-13  
**Версия**: v0.1.43  
**Последний коммит**: 1ac83c8
