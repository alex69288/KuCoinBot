# 🖥️ Локальная разработка бота

Эта инструкция объясняет, как запустить бота локально для быстрой разработки и тестирования без необходимости загрузки на сервер.

## 📋 Требования

- Python 3.9 или выше
- pip (менеджер пакетов Python)
- Git (для работы с репозиторием)

## 🚀 Пошаговая инструкция

### 1. Установка зависимостей

Откройте терминал PowerShell в корневой папке проекта и выполните:

```powershell
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# API KuCoin
KUCOIN_API_KEY=your_api_key_here
KUCOIN_SECRET_KEY=your_secret_key_here
KUCOIN_PASSPHRASE=your_passphrase_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Настройки Web App (для локальной разработки)
WEBAPP_URL=http://localhost:8000
PORT=8000

# Режим разработки (опционально)
DEBUG=True
```

**⚠️ ВАЖНО:** 
- Файл `.env` уже добавлен в `.gitignore` и не будет загружаться в репозиторий
- Никогда не публикуйте ваши API ключи публично
- Для получения API ключей KuCoin перейдите на https://www.kucoin.com/account/api
- Для создания Telegram бота используйте @BotFather в Telegram

### 3. Запуск бота

#### Вариант А: Запуск с Web App (рекомендуется)

```powershell
python main_with_webapp.py
```

Это запустит:
- Торгового бота
- Web App сервер на http://localhost:8000
- Telegram бот

#### Вариант Б: Запуск только торгового бота

Если вам не нужен веб-интерфейс:

```powershell
python -m core.bot
```

### 4. Проверка работы

После запуска вы увидите:

```
🔍 Проверка переменных окружения...
✅ Все переменные окружения настроены
🚀 Запуск торгового бота...
🌐 Запуск Web App сервера...
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Откройте браузер и перейдите на http://localhost:8000 для доступа к веб-интерфейсу.

## 🧪 Тестирование

### Запуск тестов

Перед внесением изменений рекомендуется запустить тесты:

```powershell
# Проверка подключения к API
python tests/check_env.py

# Тестовый запуск
python tests/main.py

# Полная проверка развертывания
python tests/test_full_deployment.py
```

### Тестирование конкретных функций

```powershell
# Проверка получения данных
python tests/debug_fetch.py

# Очистка состояния позиций
python tests/clean_position_state.py
```

## 🔧 Режим разработки

### Автоматическая перезагрузка

Для автоматической перезагрузки при изменении файлов используйте `watchdog`:

```powershell
# Установка (если еще не установлен)
pip install watchdog

# Запуск с автоперезагрузкой
watchmedo auto-restart --patterns="*.py" --recursive -- python main_with_webapp.py
```

### Отладка

Для включения подробного логирования добавьте в `.env`:

```env
DEBUG=True
LOG_LEVEL=DEBUG
```

## 📊 Мониторинг

### Просмотр логов

Логи сохраняются в папку `logs/`:

```powershell
# Просмотр последних логов
Get-Content logs\bot.log -Tail 50 -Wait

# Просмотр логов с ошибками
Select-String -Path logs\bot.log -Pattern "ERROR"
```

### Проверка состояния

```powershell
# Просмотр текущих позиций
Get-Content position_state.json | ConvertFrom-Json | ConvertTo-Json -Depth 10

# Просмотр настроек бота
Get-Content bot_settings.json | ConvertFrom-Json | ConvertTo-Json -Depth 10
```

## 🔄 Рабочий процесс разработки

### 1. Внесение изменений

```powershell
# Создайте новую ветку для изменений
git checkout -b feature/your-feature-name

# Внесите изменения в код
# Сохраните файлы
```

### 2. Тестирование локально

```powershell
# Запустите бота локально
python main_with_webapp.py

# Проверьте функциональность
# Убедитесь, что нет ошибок
```

### 3. Коммит изменений

```powershell
# Добавьте изменения
git add .

# Создайте коммит с осмысленным сообщением
git commit -m "[v0.0.X] Описание изменений"
```

### 4. Отправка на сервер

```powershell
# Отправьте изменения в репозиторий
git push origin feature/your-feature-name

# После этого создайте Pull Request или слейте в main
```

## 🐛 Решение проблем

### Ошибка: ModuleNotFoundError

```powershell
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Ошибка: Port already in use

```powershell
# Найдите процесс, использующий порт 8000
Get-NetTCPConnection -LocalPort 8000

# Завершите процесс
Stop-Process -Id <PID>

# Или используйте другой порт в .env
# PORT=8001
```

### Ошибка подключения к KuCoin API

- Проверьте правильность API ключей в `.env`
- Убедитесь, что IP адрес добавлен в белый список на KuCoin
- Проверьте права доступа API ключа (нужны права на торговлю)

### Telegram бот не отвечает

- Проверьте правильность `TELEGRAM_BOT_TOKEN`
- Убедитесь, что `TELEGRAM_CHAT_ID` указан верно
- Проверьте, что бот запущен через @BotFather

## 💡 Полезные советы

### Использование виртуального окружения

Рекомендуется использовать виртуальное окружение:

```powershell
# Создание виртуального окружения
python -m venv venv

# Активация (PowerShell)
.\venv\Scripts\Activate.ps1

# Установка зависимостей
pip install -r requirements.txt
```

### Быстрый рестарт

Создайте скрипт `start.ps1` для быстрого запуска:

```powershell
# start.ps1
Write-Host "🚀 Запуск бота..." -ForegroundColor Green
python main_with_webapp.py
```

Запуск:

```powershell
.\start.ps1
```

### Работа с несколькими конфигурациями

Создайте разные `.env` файлы для разных окружений:

- `.env.development` - для разработки
- `.env.staging` - для тестирования
- `.env.production` - для продакшена (на сервере)

## 🔗 Дополнительные ресурсы

- [Документация KuCoin API](https://www.kucoin.com/docs-new)
- [Документация Telegram Bot API](https://core.telegram.org/bots/api)
- [README проекта](../README.md)
- [Инструкция по деплою на Amvera](DEPLOYMENT_STATUS.md)

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Решение проблем" выше
2. Просмотрите логи в папке `logs/`
3. Запустите тесты из папки `tests/`
4. Проверьте актуальность зависимостей: `pip install -r requirements.txt --upgrade`

---

**Удачной разработки! 🚀**
