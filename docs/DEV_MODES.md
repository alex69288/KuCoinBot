# 🚀 Режимы запуска проекта

## 3️⃣ Варианта запуска

### 1️⃣ **Обычный режим** (Production-like)
```powershell
python main_local.py
```

**Что это:**
- Полная инициализация бота
- Бот запускается один раз при старте
- API перезагружается при перезагрузке сервера
- **Нужен перезапуск при любых изменениях**

**Когда использовать:**
- Тестирование всей системы
- Проверка интеграции
- Production-like сценарии

---

### 2️⃣ **Dev режим (Горячая перезагрузка)** ⭐ **РЕКОМЕНДУЕТСЯ ДЛЯ РАЗРАБОТКИ**

#### Способ 1: PowerShell скрипт (Windows)
```powershell
# Сначала один раз (если нужно):
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Затем просто:
.\dev_mode.ps1
```

#### Способ 2: Python напрямую
```powershell
python dev_mode.py
```

#### Способ 3: Uvicorn напрямую
```powershell
# Если уже инициализирован бот в другом окне
uvicorn webapp.server_dev:app --reload --reload-dirs webapp --host 0.0.0.0 --port 8000
```

**Что это:**
- API перезагружается **автоматически** при изменении файлов
- Бот инициализируется один раз
- **Не нужен перезапуск для разработки API**
- **Мониторит только папку `webapp/`**

**Что будет перезагружено:**
- ✅ `webapp/server.py` - Endpoints
- ✅ `webapp/api_compact_responses.py` - Компактные ответы
- ✅ Все файлы в `webapp/`

**Что НЕ будет перезагружено (нужен ручной перезапуск):**
- ❌ `core/bot.py` - Основной бот
- ❌ `strategies/` - Стратегии
- ❌ `config/` - Конфигурация
- ❌ `utils/` - Утилиты
- ❌ `telegram/` - Телеграм бот

**Когда использовать:**
- Разработка и изменение API endpoints
- Отладка endpoint функций
- Быстрые итерации при работе с API

---

### 3️⃣ **Manual режим (только web app)**

Если вы уже запустили бота отдельно:

```powershell
# Окно 1 - Запускаем основной бот
python main_local.py

# Окно 2 - Запускаем только web app с перезагрузкой
uvicorn webapp.server_dev:app --reload --reload-dirs webapp --port 8000
```

---

## 📊 Сравнение режимов

| Параметр | main_local.py | dev_mode.ps1 | Manual uvicorn |
|----------|---------------|--------------|-----------------|
| Инициализация бота | ✅ Вместе | ✅ Один раз | ❌ Отдельно |
| Hot reload API | ❌ Нет | ✅ Да | ✅ Да |
| Перезагрузка бота | ❌ Нет | ❌ Нет | ❌ Нет |
| Просто для запуска | ✅ Да | ❌ Нет | ❌ Нет |
| Для разработки API | ❌ Нет | ✅ Да | ✅ Да |

---

## 🔧 Какие изменения требуют перезагрузки?

### ❌ Требуется перезагрузка `Ctrl+C` и `python main_local.py`:

```python
# core/bot.py - Изменения логики бота
def start_trading_loop(self):
    # ... изменения здесь
    pass

# config/settings.py - Изменения конфигурации
RISK_PERCENTAGE = 5.0  # Изменили с 3.0

# strategies/ema_ml.py - Изменения стратегии
def check_ema_strategy(self):
    # ... изменения здесь
    pass

# telegram/bot.py - Изменения Telegram бота
def send_message(self, text):
    # ... изменения здесь
    pass
```

### ✅ НЕ требуется перезагрузка (Hot Reload работает):

```python
# webapp/server.py - Новый endpoint
@app.get("/api/new-endpoint")
async def new_endpoint():
    return {"data": "new"}

# webapp/api_compact_responses.py - Изменение format
def compact_status_response(positions):
    # ... изменили формат
    pass

# webapp/handlers.py - Изменение обработчика
async def handle_request():
    # ... изменения здесь
    pass
```

---

## 💡 Рекомендации

### Для разработки API:
```powershell
# Используй dev_mode
.\dev_mode.ps1
# или
python dev_mode.py
```

**Плюсы:**
- ⚡ Быстрая разработка
- 🔄 Автоматическая перезагрузка
- 📝 Видно, что меняется

### Для тестирования всей системы:
```powershell
# Используй main_local.py
python main_local.py
```

**Плюсы:**
- ✅ Всё работает вместе
- 🤖 Бот включен полностью
- 📊 Видна полная интеграция

---

## ⚡ Quick Tips

**Если меняешь API endpoints:**
```powershell
.\dev_mode.ps1  # Hot reload сработает за 1-2 сек
```

**Если меняешь логику бота:**
```powershell
# Сначала остановить (Ctrl+C)
python main_local.py  # Перезапустить
```

**Хочешь видеть логи:**
```powershell
# Все режимы выводят логи в консоль
# Плюс записываются в logs/bot_YYYYMMDD.log
Get-Content logs/bot_*.log -Tail 50  # Последние 50 строк
```

---

**Версия:** v0.1.14  
**Дата:** 13 ноября 2025 г.
