# 🎉 WATCH MODE - Полная автоматическая перезагрузка | v0.1.15

## ✅ Что было сделано

Создан **WATCH MODE** - режим разработки с полной автоматической перезагрузкой:

### 📦 Новые файлы:
1. **`watch_mode.py`** - Основной скрипт с мониторингом файлов
2. **`watch_mode.ps1`** - PowerShell скрипт для удобного запуска
3. **`docs/WATCH_MODE.md`** - Полная документация
4. **`WATCH_MODE_QUICKSTART.md`** - Быстрый старт

### 🔧 Обновлены:
1. **`docs/DEV_MODES.md`** - Добавлена информация о watch mode
2. **`requirements.txt`** - Уже содержит `watchdog`

---

## 🚀 Как использовать

### Способ 1: PowerShell (Windows) ⭐ **РЕКОМЕНДУЕТСЯ**
```powershell
.\watch_mode.ps1
```

### Способ 2: Python напрямую
```powershell
python watch_mode.py
```

---

## 🎯 Что произойдёт

```
1. Запусти скрипт
   ↓
2. Проект инициализируется один раз
   ↓
3. Сервер запущен и работает
   ↓
4. Ты редактируешь любой файл (webapp/, core/, strategies/ и т.д.)
   ↓
5. Сохраняешь файл (Ctrl+S)
   ↓
6. ⏱️ Подожди 1-2 сек (debounce)
   ↓
7. 🔄 Проект автоматически перезагружается
   ↓
8. Сервер работает с новым кодом! ✅
   ↓
9. Повторяй шаги 4-8 сколько угодно раз
```

---

## 📊 Что мониторится

### Папки ✅
- `core/` - Логика бота
- `strategies/` - Стратегии
- `webapp/` - API
- `config/` - Конфигурация
- `telegram/` - Telegram
- `utils/` - Утилиты
- `analytics/` - Аналитика
- `ml/` - ML модели
- `tests/` - Тесты

### Корневые файлы ✅
- `main_local.py`
- `main_server.py`
- `main_with_webapp.py`

### Расширения файлов ✅
- `.py` - Python
- `.json` - Конфиги
- `.yaml` / `.yml` - YAML
- `.html` / `.css` / `.js` - Веб

### Игнорируется ❌
- `__pycache__/`
- `.git/`
- `node_modules/`
- `.pytest_cache/`
- и т.д.

---

## 💡 Практические примеры

### Пример 1: Добавил новый endpoint

```python
# webapp/server.py
@app.get("/api/analyze")
async def analyze_market():
    return {"analysis": "done"}
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `webapp/server.py`
3. Сохрани: `Ctrl+S`
4. Подожди 1-2 сек
5. 🔄 Проект перезагружается
6. ✅ Попробуй новый endpoint!

### Пример 2: Изменил стратегию

```python
# strategies/ema_ml.py
def check_ema_strategy(self):
    # Новая логика торговли
    return {"action": "hold"}
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `strategies/ema_ml.py`
3. Сохрани: `Ctrl+S`
4. Подожди 1-2 сек
5. 🔄 Проект перезагружается с новой стратегией
6. ✅ Бот работает с новой логикой!

### Пример 3: Исправил конфигурацию

```python
# config/settings.py
RISK_PERCENTAGE = 7.5  # Увеличил с 5.0
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `config/settings.py`
3. Сохрани: `Ctrl+S`
4. Подожди 1-2 сек
5. 🔄 Проект перезагружается
6. ✅ Новые настройки применены!

---

## 🎓 Как это работает внутри

### Архитектура
```
Watchdog Library (следит за файлами)
    ↓
FileSystemEventHandler (реагирует на события)
    ├─ on_modified() - Файл изменился
    ├─ on_created() - Файл создан
    └─ on_deleted() - Файл удалён
    ↓
Debounce Timer (ждёт 1 сек)
    ↓
restart_process()
    ├─ Останавливает старый процесс (terminate + kill)
    ├─ Жда завершения
    └─ Запускает новый процесс (python main_local.py)
    ↓
should_watch_file() - Проверяет расширение и папку
    ├─ Игнорирует __pycache__, .git и т.д.
    └─ Отслеживает только нужные файлы
```

### Debouce (важно!)
```
Ты сохранил 5 раз за 1 сек:
❌ Будет 5 перезагрузок? Нет!
✅ Будет 1 перезагрузка через 1 сек

Это экономит ресурсы и время
```

---

## 📊 Сравнение всех режимов

| Режим | Команда | Hot Reload | Полнота | Для чего |
|-------|---------|-----------|---------|----------|
| **Watch Mode** ⭐ | `.\watch_mode.ps1` | ✅ Полный | ✅ Весь проект | **Основная разработка** |
| Dev Mode | `.\dev_mode.ps1` | ✅ Частичный | ⚠️ Только webapp/ | API разработка |
| Normal | `python main_local.py` | ❌ Нет | ✅ Весь проект | Тестирование |

---

## ⚙️ Настройки (расширенные)

### Изменить время дебаунса

Если перезагрузка происходит слишком часто/редко:

```python
# watch_mode.py, строка ~50
self.debounce_time = 2.0  # Измени с 1.0 на 2.0 сек
```

### Добавить новую папку

```python
# watch_mode.py, строка ~180
watch_dirs = [
    'core', 'strategies', 'webapp', 'config', 'telegram', 'utils',
    'analytics', 'ml', 'deploy', 'tests',
    'my_folder'  # ← Добавь
]
```

### Игнорировать папку

```python
# watch_mode.py, строка ~44
IGNORE_PATTERNS = {
    '__pycache__', '.git', '.vscode', '.pytest_cache', 'node_modules',
    '.pyc', '.pyo', '.egg-info', 'build', 'dist', '.env.local',
    'temp'  # ← Добавь
}
```

---

## 🔥 Лучшие практики

### ✅ Делай так:
1. Запусти `.\watch_mode.ps1` в начале дня
2. Редактируй файлы
3. Сохраняй (Ctrl+S)
4. Проект перезагружается автоматически
5. Проверяй логи в реальном времени

### ❌ Не делай так:
1. Не запускай несколько watch mode одновременно
2. Не меняй `requirements.txt` (нужна переустановка вручную)
3. Не забывай про `.env` (нужен ручной перезапуск)
4. Не паникуй, если перезагрузка задерживается на 1-2 сек

---

## 🛟 Решение проблем

### Проблема: ModuleNotFoundError: watchdog
**Решение:**
```powershell
pip install watchdog
```

### Проблема: Проект не перезагружается
**Решение:**
1. Проверь, что файл находится в папке `core/`, `strategies/`, `webapp/` и т.д.
2. Проверь расширение файла (`.py`, `.json`, `.yaml`)
3. Перезапусти watch mode (Ctrl+C и `.\watch_mode.ps1` снова)

### Проблема: Слишком частая перезагрузка
**Решение:** Увеличь `debounce_time` (смотри "Настройки")

### Проблема: Изменения в .env не применяются
**Решение:** Нажми Ctrl+C и запусти снова `.\watch_mode.ps1`

---

## 💾 Git коммиты

```
0e7b4c6 [v0.1.15] 🔄 Watch Mode - Полная автоматическая перезагрузка при любых изменениях
```

---

## 🎯 Итоговая статистика

| Метрика | Значение |
|---------|----------|
| Новых файлов | 4 |
| Обновлённых файлов | 2 |
| Строк кода | ~400 |
| Функциональность | Watch всего проекта |
| Время разработки | Экономит ~10-15 мин в час |

---

## ✨ Преимущества Watch Mode

1. **⚡ Скорость** - Не нужно вручную перезапускать
2. **🎯 Удобство** - Просто сохрани и вперёд
3. **📊 Видимость** - Сразу видишь результаты
4. **🔄 Автоматизм** - Всё происходит само
5. **💰 Экономия** - Экономит время на разработку

---

## 🚀 Как начать

```powershell
# Запусти один раз
.\watch_mode.ps1

# Готово! Теперь просто редактируй файлы
# Сохраняй (Ctrl+S)
# Проект перезагружается автоматически 🔄
```

---

**Версия:** v0.1.15  
**Дата:** 13 ноября 2025 г.  
**Статус:** ✅ ПОЛНОСТЬЮ ГОТОВО  
**Протестировано:** ✅ ДА
