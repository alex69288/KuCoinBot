# 🚀 Деплой на Amvera - Пошаговая инструкция

## Подготовка проекта

### 1. Убедись что все изменения закоммичены:

```bash
git status
git add .
git commit -m "[v0.1.23] Подготовка к деплою на Amvera, смена порта на 3001"
git push
```

---

## Деплой на Amvera

### 1. Зарегистрируйся на Amvera:
- Перейди на https://amvera.ru
- Зарегистрируйся или войди через GitHub

### 2. Создай новый проект:
- Нажми **"Создать проект"**
- Выбери **"Из Git репозитория"**
- Подключи свой GitHub аккаунт
- Выбери репозиторий: **alex69288/KuCoinBot**
- Ветка: **main**

### 3. Конфигурация проекта:

Amvera автоматически обнаружит `amvera.yml`. Проверь настройки:

```yaml
Environment: Node.js
Version: 20
Build folder: backend
Port: 3000
```

### 4. Установи переменные окружения:

В разделе **Environment Variables** добавь:

```env
NODE_ENV=production
KUCOIN_API_KEY=твой_ключ
KUCOIN_API_SECRET=твой_секрет
KUCOIN_API_PASSPHRASE=твой_пароль
KUCOIN_TESTNET=false
TRADING_SYMBOL=BTC/USDT
TRADING_TIMEFRAME=1h
ML_SERVICE_URL=http://localhost:5000
FRONTEND_URL=https://твой-домен.amvera.app
```

⚠️ **Для тестирования можно оставить API ключи пустыми** - бот запустится в mock-режиме.

### 5. Запусти деплой:
- Нажми **"Deploy"**
- Дождись завершения сборки (2-3 минуты)

### 6. Проверь статус:

После успешного деплоя откроется URL вида:
```
https://твой-проект.amvera.app
```

Проверь health check:
```
https://твой-проект.amvera.app/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"..."}
```

---

## Тестирование на Amvera

### Проверка API:

```bash
# Статус бота
curl https://твой-проект.amvera.app/api/status

# Рыночные данные
curl https://твой-проект.amvera.app/api/market

# Запустить бота
curl -X POST https://твой-проект.amvera.app/api/trade/start

# Включить торговлю
curl -X POST https://твой-проект.amvera.app/api/trade/enable
```

---

## Локальное тестирование на порту 3001

Пока Amvera деплоится, можешь протестировать локально на новом порту:

```powershell
# Останови старый процесс на порту 3000
# Запусти на 3001:
cd c:\Users\user\Documents\Scripts\KuCoinBotV4Copilot\backend
npm run dev
```

Теперь API доступен на:
```
http://localhost:3001/health
http://localhost:3001/api/status
```

Проверка:
```powershell
Invoke-RestMethod -Uri http://localhost:3001/health
```

---

## Frontend для Amvera

После деплоя Backend, можно задеплоить Frontend:

### Вариант 1: Отдельный проект на Amvera

1. Создай новый проект на Amvera
2. Конфигурация:
   ```yaml
   environment: nodejs
   version: 20
   folder: frontend
   build: npm install && npm run build
   run: npm run preview
   port: 4173
   ```

3. Environment Variables:
   ```env
   VITE_API_URL=https://backend-проект.amvera.app
   ```

### Вариант 2: Vercel (проще для фронтенда)

1. Зайди на https://vercel.com
2. Import Git Repository
3. Выбери папку `frontend`
4. Environment Variables:
   ```env
   VITE_API_URL=https://backend-проект.amvera.app
   ```

---

## Мониторинг на Amvera

### Логи:
- В Amvera Dashboard → **Logs**
- Смотри логи бота в реальном времени

### Метрики:
- CPU usage
- Memory usage
- Request count

### Перезапуск:
Если бот завис - просто нажми **"Restart"** в Dashboard

---

## Troubleshooting

### ❌ Build failed

**Проверь:**
- `backend/package.json` содержит `"build": "tsc"`
- `backend/tsconfig.json` корректный
- Все зависимости в `dependencies`, не в `devDependencies`

### ❌ Health check failed

**Проверь:**
- Порт в `amvera.yml` = порт в коде (3000)
- Endpoint `/health` работает
- Нет ошибок в логах

### ❌ API timeout

**Возможно:**
- KuCoin API недоступен (используй mock-режим)
- Нужно добавить retry логику
- Увеличь timeout в `exchange.ts`

---

## Готово! 🎉

После деплоя у тебя будет:
- ✅ Backend API на Amvera
- ✅ Реальный URL для тестирования
- ✅ Автоматические деплои при push в main
- ✅ Логи и мониторинг

Начни деплой прямо сейчас! 🚀
