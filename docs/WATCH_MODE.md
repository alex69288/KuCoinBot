# 🔄 WATCH MODE - Полная автоматическая перезагрузка

## 🎯 Что это?

**Watch Mode** запускает проект один раз, а затем **автоматически перезагружает его** при **любом изменении** в коде!

```
Запуск один раз → Проект работает → Редактируешь файл → 
Сохраняешь (Ctrl+S) → Проект перезагружается → Готово! ✅
```

---

## 🚀 Как использовать?

### Способ 1: PowerShell (Windows) ⭐ **РЕКОМЕНДУЕТСЯ**

```powershell
# Запусти скрипт
.\watch_mode.ps1

# Вот и всё! Теперь:
# - Отредактируй любой файл
# - Сохрани (Ctrl+S)
# - Проект перезагружается автоматически! 🔄
```

### Способ 2: Python напрямую

```powershell
python watch_mode.py
```

### Способ 3: Если watchdog не установлен

```powershell
# Установи watchdog
pip install watchdog

# Затем запусти
python watch_mode.py
```

---

## 📊 Сравнение всех режимов

| Режим | Команда | Hot Reload | Что перезагружается |
|-------|---------|-----------|---------------------|
| **Watch Mode** ⭐ | `.\watch_mode.ps1` | ✅ Полностью | Весь проект |
| **Dev Mode** | `.\dev_mode.ps1` | ✅ Частично | Только `webapp/` |
| **Normal** | `python main_local.py` | ❌ Нет | - |

---

## 🎓 Как это работает?

```
Watch Mode (watchdog library)
    ↓
Следит за файлами проекта
    ├── core/
    ├── strategies/
    ├── webapp/
    ├── config/
    ├── telegram/
    ├── utils/
    └── etc...
    ↓
Файл изменился? (Ctrl+S)
    ↓
Ждёт 1 сек (debounce) - чтоб не перезагружаться 100 раз при быстром сохранении
    ↓
Останавливает текущий процесс
    ↓
Запускает новый процесс (python main_local.py)
    ↓
Проект запущен с новым кодом! ✅
```

---

## 📋 Мониторится

### Папки
- ✅ `core/` - Логика бота
- ✅ `strategies/` - Стратегии торговли
- ✅ `webapp/` - API endpoints
- ✅ `config/` - Конфигурация
- ✅ `telegram/` - Telegram бот
- ✅ `utils/` - Утилиты
- ✅ `analytics/` - Аналитика
- ✅ `ml/` - ML модели
- ✅ `tests/` - Тесты

### Файлы в корне
- ✅ `main_local.py`
- ✅ `main_server.py`
- ✅ `main_with_webapp.py`
- ✅ `.env` (изменения видны при следующем перезапуске)

### Расширения файлов
- ✅ `.py` - Python файлы
- ✅ `.json` - JSON конфиги
- ✅ `.yaml` / `.yml` - YAML конфиги
- ✅ `.html` / `.css` / `.js` - Веб файлы

---

## ⚠️ Не мониторится (игнорируется)

- ❌ `__pycache__/`
- ❌ `.git/`
- ❌ `.vscode/`
- ❌ `.pytest_cache/`
- ❌ `node_modules/`
- ❌ `*.pyc`
- ❌ `*.egg-info`
- ❌ `build/`
- ❌ `dist/`

---

## 🎯 Практические примеры

### Пример 1: Добавил новый endpoint

```python
# webapp/server.py
@app.get("/api/my-new-endpoint")
async def my_new_endpoint():
    return {"status": "ok"}
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `webapp/server.py`
3. Сохрани: `Ctrl+S`
4. ⏱️ Подожди 1-2 сек
5. 🔄 Проект перезагружается
6. ✅ Новый endpoint уже работает!

### Пример 2: Изменил стратегию торговли

```python
# strategies/ema_ml.py
def check_ema_strategy(self):
    # Изменил логику
    return {"decision": "sell"}
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `strategies/ema_ml.py`
3. Сохрани: `Ctrl+S`
4. ⏱️ Подожди 1-2 сек
5. 🔄 Проект перезагружается с новой стратегией
6. ✅ Бот работает с новой логикой!

### Пример 3: Исправил конфигурацию

```python
# config/settings.py
RISK_PERCENTAGE = 5.0  # Изменил с 3.0
```

**Действие:**
1. Запусти: `.\watch_mode.ps1`
2. Отредактируй `config/settings.py`
3. Сохрани: `Ctrl+S`
4. ⏱️ Подожди 1-2 сек
5. 🔄 Проект перезагружается
6. ✅ Новые настройки применены!

---

## 💡 Советы

### Совет 1: Используй для разработки
```powershell
# Для полной разработки - watch mode лучший выбор
.\watch_mode.ps1
```

### Совет 2: Не забывай про .env
```powershell
# Если меняешь .env - нужно вручную перезапустить
# (watch mode не перезагружает .env переменные)
# Нажми Ctrl+C и запусти заново
```

### Совет 3: Дебаунс срабатывает автоматически
```
Если сохранишь 5 раз за 1 сек:
❌ Не будет 5 перезагрузок
✅ Будет 1 перезагрузка через 1 сек
```

### Совет 4: Смотри логи в реальном времени
```powershell
# В отдельном окне PowerShell
Get-Content logs/bot_*.log -Wait
```

---

## 🔧 Расширенные настройки

### Изменить время дебаунса

Если перезагрузка происходит слишком часто:

```python
# watch_mode.py, строка ~49
self.debounce_time = 2.0  # Измени с 1.0 на 2.0 сек
```

### Добавить новую папку для мониторинга

```python
# watch_mode.py, строка ~179
watch_dirs = [
    'core', 'strategies', 'webapp', 'config', 'telegram', 'utils',
    'analytics', 'ml', 'deploy', 'tests',
    'your_new_folder'  # ← Добавь здесь
]
```

### Игнорировать файлы

```python
# watch_mode.py, строка ~43
IGNORE_PATTERNS = {
    '__pycache__', '.git', '.vscode', '.pytest_cache', 'node_modules',
    '.pyc', '.pyo', '.egg-info', 'build', 'dist', '.env.local',
    'my_temp_folder'  # ← Добавь здесь
}
```

---

## ❓ FAQ

**Q: Почему перезагрузка задерживается на 1-2 сек?**
A: Это дебаунс — чтобы избежать множественных перезагрузок при быстрых изменениях.

**Q: Что если я меняю requirements.txt?**
A: Нужно остановить (Ctrl+C) и переустановить: `pip install -r requirements.txt`, затем снова запустить.

**Q: Работает ли на Linux и Mac?**
A: Да! Watch mode кроссплатформенный, работает везде.

**Q: Можно ли использовать это в Production?**
A: Нет! Watch mode только для разработки. Для production используй `main_server.py`.

---

## 🚀 Итог

**Watch Mode - идеальное решение для разработки:**
- 🚀 Запустил один раз
- 💾 Редактируешь файлы
- 🔄 Проект сам перезагружается
- ✅ Видишь изменения сразу

```powershell
.\watch_mode.ps1  # И вперёд! 🚀
```

---

**Версия:** v0.1.15  
**Дата:** 13 ноября 2025 г.  
**Статус:** ✅ ГОТОВО
