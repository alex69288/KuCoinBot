# 🔥 CHEAT SHEET - Все режимы в одном месте

## ⚡ Самое важное

```powershell
# ГЛАВНАЯ КОМАНДА ДЛЯ РАЗРАБОТКИ
.\watch_mode.ps1

# Готово! Просто редактируй файлы и сохраняй (Ctrl+S)
# Проект сам перезагружается 🔄
```

---

## 📋 Все режимы

### 1️⃣ WATCH MODE ⭐ (Полная автоматическая перезагрузка)
```powershell
.\watch_mode.ps1
```
**Когда:** Разработка всего проекта  
**Перезагрузка:** Автоматическая при любых изменениях  
**Время перезагрузки:** 1-2 сек  
**Лучше всего для:** Быстрой разработки

### 2️⃣ DEV MODE (Hot reload только для API)
```powershell
.\dev_mode.ps1
```
**Когда:** Разработка только API endpoints  
**Перезагрузка:** Автоматическая только для `webapp/`  
**Время перезагрузки:** 1 сек  
**Лучше всего для:** API разработки

### 3️⃣ NORMAL MODE (Полный контроль)
```powershell
python main_local.py
```
**Когда:** Тестирование системы  
**Перезагрузка:** Ручная (Ctrl+C и заново)  
**Время запуска:** 40-50 сек  
**Лучше всего для:** Полного тестирования

---

## 🎯 Быстрый выбор по задаче

| Задача | Команда | Перезагрузка |
|--------|---------|--------------|
| Разрабатываю новый endpoint | `.\watch_mode.ps1` | ✅ Автоматическая |
| Изменяю стратегию торговли | `.\watch_mode.ps1` | ✅ Автоматическая |
| Исправляю конфигурацию | `.\watch_mode.ps1` | ✅ Автоматическая |
| Тестирую весь проект | `python main_local.py` | ❌ Ручная |
| Хочу быстро развивать API | `.\dev_mode.ps1` | ✅ Для API |

---

## 💡 Частые сценарии

### Сценарий 1: "Я хочу просто разрабатывать"
```powershell
.\watch_mode.ps1  # Запусти это
# Редактируй файлы → Ctrl+S → Готово! 🚀
```

### Сценарий 2: "Я разработчик API, хочу быстро"
```powershell
.\dev_mode.ps1  # Запусти это
# Меняешь только webapp/ → Ctrl+S → Быстрая перезагрузка ⚡
```

### Сценарий 3: "Я тестирую весь бот"
```powershell
python main_local.py  # Запусти это
# Проверяешь всё → Ctrl+C при нужде → Заново запускаешь
```

---

## 🛑 Остановка всех режимов

```powershell
Ctrl+C
```

---

## 📊 Матрица времени

| Действие | Время | Режим |
|----------|-------|-------|
| Запуск | 40-50 сек | main_local.py |
| Запуск | ~5 сек | watch_mode / dev_mode |
| Hot reload | 1-2 сек | watch_mode / dev_mode |
| Ручная перезагрузка | 40-50 сек | main_local.py |

---

## 🚀 Профессиональный workflow

```
Начало дня:
  .\watch_mode.ps1  ← Запустил один раз

Разработка (8 часов):
  1. Открыл файл
  2. Отредактировал
  3. Сохранил (Ctrl+S)
  4. Проект перезагружается
  5. Проверил результат
  6. Повтори

Конец дня:
  Ctrl+C  ← Остановил сервер
```

---

## ❌ Что НЕ перезагружается

Во ВСЕ режимах:
- ❌ `requirements.txt` - Нужна переустановка вручную
- ❌ `.env` файл - Загружается только при старте

---

## ✅ Что перезагружается

### Watch Mode
- ✅ `core/` - Весь код бота
- ✅ `strategies/` - Все стратегии
- ✅ `webapp/` - Все endpoints
- ✅ `config/` - Конфигурация
- ✅ `telegram/` - Telegram бот
- ✅ `utils/` - Утилиты
- ✅ Всё остальное

### Dev Mode
- ✅ Только `webapp/` - API endpoints

---

## 🎓 Логика разработки

```
Watch Mode:
  Один запуск → Проект работает → Меняешь что угодно → 
  Сохраняешь → Проект перезагружается → Видишь результат

Dev Mode:
  Один запуск → Бот инициализирован → Меняешь API → 
  Сохраняешь → API перезагружается → Тестируешь endpoint

Normal Mode:
  Каждый раз новый запуск → Полная инициализация → 
  Меняешь что-то → Нужен новый запуск
```

---

## 🔧 Для опытных

### Изменить deboounce в watch mode
```python
# watch_mode.py, строка ~50
self.debounce_time = 2.0  # Увеличить до 2 сек
```

### Запустить только на определённых файлах
```python
# watch_mode.py, строка ~45
WATCH_EXTENSIONS = {'.py', '.json'}  # Только Python и JSON
```

### Отключить мониторинг папки
```python
# watch_mode.py, строка ~180
watch_dirs = ['core', 'strategies', 'webapp', 'config', 'telegram', 'utils']
# Удали лишние папки из списка
```

---

## 🚨 Решение быстрых проблем

| Проблема | Решение |
|----------|---------|
| Проект не запускается | `python watch_mode.py` вместо `.\watch_mode.ps1` |
| Watchdog не найден | `pip install watchdog` |
| Медленная перезагрузка | Проверь RAM/CPU использование |
| .env не применяется | Ctrl+C и заново `.\watch_mode.ps1` |
| Много файлов → медленно | Используй `dev_mode.ps1` вместо `watch_mode.ps1` |

---

## 📚 Дополнительные ресурсы

- 📖 `docs/WATCH_MODE.md` - Полная документация Watch Mode
- 📖 `docs/DEV_MODES.md` - Сравнение всех режимов
- 📖 `WATCH_MODE_QUICKSTART.md` - Быстрый старт
- 📖 `WATCH_MODE_REPORT_v0.1.15.md` - Детальный отчёт

---

## 🎉 TL;DR (Слишком долго, не читал)

```powershell
# Запусти это
.\watch_mode.ps1

# Редактируй файлы
# Сохраняй (Ctrl+S)
# Проект сам перезагружается

# Остановка
Ctrl+C
```

**Версия:** v0.1.15  
**Статус:** ✅ ГОТОВО  
**Последнее обновление:** 13 ноября 2025 г.
