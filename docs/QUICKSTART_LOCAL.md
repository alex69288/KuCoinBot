# ⚡ Быстрый старт - Локальная разработка

## 🚀 Запуск за 3 шага

### 1️⃣ Установка зависимостей

```powershell
pip install -r requirements.txt
```

### 2️⃣ Настройка .env

Скопируйте `.env.example` в `.env` и заполните:

```env
KUCOIN_API_KEY=ваш_api_ключ
KUCOIN_SECRET_KEY=ваш_секретный_ключ
KUCOIN_PASSPHRASE=ваша_парольная_фраза
TELEGRAM_BOT_TOKEN=токен_вашего_бота
TELEGRAM_CHAT_ID=ваш_chat_id
WEBAPP_URL=http://localhost:8000
```

### 3️⃣ Запуск

```powershell
# Автоматический запуск (проверит все зависимости)
.\start_local.ps1

# Или просто запустите бота
python main_with_webapp.py
```

✅ **Готово!** Бот запущен на http://localhost:8000

---

## 📋 Полезные команды

### Тестирование

```powershell
# Проверка переменных окружения
python tests/check_env.py

# Тестовый запуск
python tests/main.py

# Проверка подключения к API
python tests/debug_fetch.py
```

### Просмотр логов

```powershell
# Последние 50 строк логов
Get-Content logs\bot.log -Tail 50

# Логи в реальном времени
Get-Content logs\bot.log -Tail 50 -Wait

# Только ошибки
Select-String -Path logs\bot.log -Pattern "ERROR"
```

### Управление состоянием

```powershell
# Просмотр текущих позиций
Get-Content position_state.json | ConvertFrom-Json | ConvertTo-Json

# Очистка состояния
python tests/clean_position_state.py
```

### Git команды

```powershell
# Проверка изменений
git status

# Коммит изменений
git add .
git commit -m "[v0.0.X] Описание"
git push
```

---

## 🔧 Решение проблем

### ❌ Порт занят

```powershell
# Найти процесс на порту 8000
Get-NetTCPConnection -LocalPort 8000

# Завершить процесс
Stop-Process -Id <PID>
```

### ❌ Модуль не найден

```powershell
pip install -r requirements.txt --force-reinstall
```

### ❌ Ошибка API

- Проверьте API ключи в `.env`
- Убедитесь, что IP в белом списке KuCoin
- Проверьте права доступа API ключа

---

## 📚 Документация

- 📖 [Полная инструкция по локальной разработке](LOCAL_DEVELOPMENT.md)
- 🚀 [Основной README](../README.md)
- ☁️ [Деплой на Amvera](DEPLOYMENT_STATUS.md)
- 🌐 [Настройка Web App](WEBAPP_GUIDE.md)

---

**Удачной разработки! 🚀**
