# 📁 Различия между файлами запуска

## 🎯 Три версии main файла

В проекте теперь есть три файла для запуска бота:

### 1. `main_local.py` - Локальная разработка 💻

**Назначение:** Для запуска на вашем компьютере во время разработки

**Особенности:**
- ✅ Загружает переменные из `.env` файла
- ✅ Показывает подробные сообщения о загрузке
- ✅ Предоставляет детальную информацию об ошибках
- ✅ Указывает локальные адреса (localhost)

**Когда использовать:**
- Разработка новых функций
- Отладка ошибок
- Тестирование изменений
- Локальное тестирование

**Запуск:**
```powershell
# Автоматически через скрипт
.\start_local.ps1

# Или вручную
python main_local.py
```

---

### 2. `main_server.py` - Серверный деплой ☁️

**Назначение:** Для запуска на облачных платформах (Amvera, Railway, Heroku)

**Особенности:**
- ❌ НЕ загружает `.env` файл
- ✅ Использует системные переменные окружения
- ✅ Оптимизирован для облачного деплоя
- ✅ Меньше информационных сообщений

**Когда использовать:**
- Деплой на Amvera.ru
- Деплой на Railway.app
- Деплой на Heroku
- Любой облачный хостинг

**Запуск на сервере:**
Автоматически через `Procfile`:
```
web: python main_server.py
```

---

### 3. `main_with_webapp.py` - Универсальная версия 🔄

**Назначение:** Автоматическое определение окружения

**Особенности:**
- 🔄 Автоматически определяет среду запуска
- ✅ Если найден `.env` - загружает его (локальный режим)
- ✅ Если `.env` отсутствует - использует системные переменные (серверный режим)
- ✅ Можно использовать везде

**Когда использовать:**
- Если не хотите думать о версии
- Универсальное решение
- Миграция между окружениями

**Запуск:**
```powershell
# Локально
python main_with_webapp.py

# На сервере
# Просто задеплойте - сработает автоматически
```

---

## 📊 Сравнительная таблица

| Параметр | `main_local.py` | `main_server.py` | `main_with_webapp.py` |
|----------|-----------------|------------------|-----------------------|
| **Загрузка .env** | ✅ Всегда | ❌ Никогда | 🔄 Автоматически |
| **Системные переменные** | ✅ Да | ✅ Да | ✅ Да |
| **Подробные логи** | ✅ Максимум | ⚠️ Стандарт | ⚠️ Стандарт |
| **Локальные URL** | ✅ localhost | ⚠️ 0.0.0.0 | 🔄 Оба |
| **Облачный деплой** | ❌ Не рекомендуется | ✅ Оптимально | ✅ Работает |
| **Локальная разработка** | ✅ Оптимально | ❌ Не работает без переменных | ✅ Работает |

---

## 🚀 Рекомендуемое использование

### Для локальной разработки:

```powershell
# Используйте start_local.ps1 (запускает main_local.py)
.\start_local.ps1
```

**Преимущества:**
- Автоматическая проверка зависимостей
- Проверка .env файла
- Подробные сообщения об ошибках
- Помощь в настройке

### Для деплоя на сервер:

`Procfile` уже настроен на `main_server.py`:
```
web: python main_server.py
```

**Преимущества:**
- Не пытается загрузить .env
- Оптимизирован для облака
- Меньше ненужных проверок

### Универсальное решение:

Если не хотите думать - используйте `main_with_webapp.py`:
```powershell
python main_with_webapp.py
```

**Преимущества:**
- Работает везде
- Автоматическое определение окружения
- Универсальность

---

## 🔧 Настройка переменных окружения

### Локально (все три файла):

1. Создайте файл `.env` в корне проекта
2. Скопируйте содержимое из `.env.example`
3. Заполните своими значениями:

```env
KUCOIN_API_KEY=ваш_ключ
KUCOIN_SECRET_KEY=ваш_секрет
KUCOIN_PASSPHRASE=ваша_фраза
TELEGRAM_BOT_TOKEN=токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
WEBAPP_URL=http://localhost:8000
PORT=8000
```

### На сервере (Amvera):

1. Откройте панель управления проектом
2. Перейдите в раздел "Переменные окружения"
3. Добавьте переменные:
   - `KUCOIN_API_KEY`
   - `KUCOIN_SECRET_KEY`
   - `KUCOIN_PASSPHRASE`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
   - `WEBAPP_URL` (URL вашего проекта)

---

## 📁 Структура файлов

```
project/
├── main_local.py          # 💻 Локальная разработка (рекомендуется)
├── main_server.py         # ☁️ Серверный деплой (рекомендуется)
├── main_with_webapp.py    # 🔄 Универсальная версия
├── start_local.ps1        # 🚀 Скрипт запуска (использует main_local.py)
├── Procfile               # ☁️ Конфигурация сервера (использует main_server.py)
├── .env                   # 🔒 Локальные переменные (не в Git)
└── .env.example           # 📄 Шаблон для .env
```

---

## 💡 Часто задаваемые вопросы

### Q: Какой файл использовать для разработки?
**A:** `main_local.py` через скрипт `.\start_local.ps1`

### Q: Какой файл используется на сервере?
**A:** `main_server.py` (настроено в Procfile)

### Q: Можно ли использовать main_with_webapp.py везде?
**A:** Да, это универсальная версия с автоопределением

### Q: Почему три разных файла?
**A:** Для оптимизации и ясности:
- `main_local.py` - максимум подсказок для разработки
- `main_server.py` - оптимизирован для продакшена
- `main_with_webapp.py` - универсальное решение

### Q: Что будет, если запустить main_server.py локально?
**A:** Будет ошибка, т.к. он не загружает .env файл

### Q: Что будет, если задеплоить main_local.py на сервер?
**A:** Будет работать, но с ненужными проверками .env файла

---

## 🎯 Итог

### Используйте:
- 💻 **Локально:** `.\start_local.ps1` → `main_local.py`
- ☁️ **На сервере:** Procfile → `main_server.py`
- 🔄 **Не уверены:** `main_with_webapp.py`

**Все файлы делают одно и то же - запускают бота. Разница только в способе загрузки переменных окружения!**

---

**Удачной разработки! 🚀**
