# Исправление подсчета количества открытых позиций [v0.1.10]

## 🔴 Проблема
При обращении к API endpoint `GET /api/status`, количество открытых позиций всегда показывало **1**, хотя в файле `position_state.json` было **2** позиции на пару BTC/USDT.

## 🔍 Анализ причины
При отправке запроса на веб-приложение с разных рабочих директорий (например, когда сервер запускается из разных путей), код использовал **относительный путь** к файлу:

```python
# ❌ НЕПРАВИЛЬНО - относительный путь
if os.path.exists('position_state.json'):
    state = load_position_state('position_state.json')
```

Когда рабочая директория (`os.getcwd()`) отличалась от корня проекта, файл не находился и условие `if os.path.exists('position_state.json')` возвращало `False`.

## 🟢 Решение
1. **Добавлена переменная PROJECT_ROOT** в `webapp/server.py`:
```python
# Определяем директорию webapp (где находится этот файл server.py)
WEBAPP_DIR = os.path.dirname(os.path.abspath(__file__))
# Корневая директория проекта (один уровень выше webapp)
PROJECT_ROOT = os.path.dirname(WEBAPP_DIR)
```

2. **Все 7 вхождений относительного пути заменены на абсолютный**:
```python
# ✅ ПРАВИЛЬНО - абсолютный путь
position_state_path = os.path.join(PROJECT_ROOT, 'position_state.json')
if os.path.exists(position_state_path):
    state = load_position_state(position_state_path)
```

## 📋 Измененные файлы

### `webapp/server.py`
- **Строки 146-149**: Добавлена переменная `PROJECT_ROOT`
- **Строки 287-289**: Использование абсолютного пути в `get_status()`
- **Строки 683-685**: Использование абсолютного пути в `get_positions()`
- **Строки 790-792**: Использование абсолютного пути в endpoint закрытия позиции
- **Строки 832-833**: Использование абсолютного пути при сохранении состояния
- **Строки 892-894**: Использование абсолютного пути в `close_all_positions()`
- **Строки 931-932**: Использование абсолютного пути при сохранении состояния
- **Строки 1637-1638**: Использование абсолютного пути в WebSocket обработчике

### `tests/test_positions_count_fix.py`
Добавлен новый тест для проверки:
- Правильного подсчета количества позиций
- Работы с абсолютными путями независимо от рабочей директории

## ✅ Результаты тестирования

```
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Исправление работает корректно.

✅ Тест 1 (подсчет позиций): ПРОЙДЕН
   - Всего открытых позиций: 2 ✅ (как и ожидалось)
   - Размер позиции BTC/USDT: 2.1 USDT

✅ Тест 2 (абсолютные пути): ПРОЙДЕН
   - Файл НЕ найден с относительным путем ✅
   - Файл найден с абсолютным путем ✅
   - Позиции считаны правильно: 2
```

## 🚀 Что изменилось для пользователя

**До исправления (v0.1.9):**
- API `/api/status` возвращал `open_count: 1` (ошибочно)
- Веб-приложение показывало "1 открытая позиция" вместо "2 открытых позиций"

**После исправления (v0.1.10):**
- API `/api/status` возвращает `open_count: 2` (правильно)
- Веб-приложение показывает "2 открытые позиции" (как на сайте)

## 🔧 Технические детали

### Почему происходила ошибка?
При использовании относительных путей в Python, файл ищется от текущей рабочей директории (`os.getcwd()`). Если приложение запускается из другой директории, путь будет неправильным.

### Почему абсолютные пути - правильное решение?
Абсолютные пути работают независимо от того, откуда запускается приложение:
```
PROJECT_ROOT = /Users/user/Documents/Scripts/KuCoinBotV4Copilot
position_state.json = /Users/user/Documents/Scripts/KuCoinBotV4Copilot/position_state.json

# Файл находится корректно из любой рабочей директории ✅
```

## 📝 Коммит
```
[v0.1.10] Исправление подсчета количества открытых позиций
- Исправлены все 7 мест использования position_state.json
- Добавлена переменная PROJECT_ROOT для абсолютных путей
- Добавлены тесты для проверки исправления
- Все тесты пройдены успешно ✅
```

---
**Дата**: 12 ноября 2025
**Версия**: v0.1.10
**Статус**: ✅ Готово к использованию
