# 🌐 Настройка Web App для Telegram бота

## ⚠️ Важно: Требования Telegram

Telegram WebApp работает **только с HTTPS URL**. Локальные адреса (http://localhost, http://127.0.0.1) **не поддерживаются**.

## 📋 Варианты развертывания

### 1. 🧪 Тестирование с Ngrok (локально)

Ngrok создает HTTPS туннель к вашему локальному серверу.

#### Установка:
```bash
# Скачайте с https://ngrok.com/download
# Или через chocolatey:
choco install ngrok
```

#### Использование:
```bash
# 1. Запустите бот локально
python main_with_webapp.py

# 2. В другом терминале запустите ngrok
ngrok http 8080

# 3. Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# 4. Добавьте в .env файл:
WEBAPP_URL=https://abc123.ngrok.io
```

#### Автоматический запуск (Windows):
```bash
start_webapp_with_ngrok.bat
```

**Важно**: Ngrok URL меняется при каждом запуске (в бесплатной версии).

---

### 2. 🚀 Продакшн на Amvera.io

Amvera.io - бесплатный хостинг с автоматическим HTTPS.

#### Шаги развертывания:

1. **Зарегистрируйтесь на [Amvera.io](https://amvera.io)**

2. **Создайте новый проект:**
   - Подключите GitHub репозиторий
   - Выберите Python окружение

3. **Настройте переменные окружения:**
   ```
   KUCOIN_API_KEY=ваш_api_key
   KUCOIN_API_SECRET=ваш_api_secret
   KUCOIN_API_PASSPHRASE=ваш_passphrase
   TELEGRAM_BOT_TOKEN=ваш_telegram_token
   TELEGRAM_CHAT_ID=ваш_chat_id
   PORT=8080
   ```
   ⚠️ **Не указывайте WEBAPP_URL** - он будет автоматически определен

4. **Деплой:**
   - Amvera автоматически соберет и запустит проект
   - После деплоя получите URL вида: `https://ваш-проект.amvera.app`

5. **Обновите .env (локально для разработки):**
   ```
   WEBAPP_URL=https://ваш-проект.amvera.app
   ```

📖 **Подробная инструкция**: [AMVERA_DEPLOYMENT.md](AMVERA_DEPLOYMENT.md)

---

### 3. 🌐 Продакшн на Railway.app

Railway - альтернативный хостинг с бесплатным тарифом.

#### Шаги:

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект из GitHub
3. Добавьте переменные окружения (как для Amvera)
4. Railway автоматически предоставит HTTPS URL
5. Обновите `WEBAPP_URL` в .env

---

## 🔧 Проверка настройки

После настройки WEBAPP_URL:

1. **Перезапустите бота:**
   ```bash
   python main_with_webapp.py
   ```

2. **Проверьте логи:**
   - Должно быть сообщение: `✅ Кнопка Web App отправлена`
   - Не должно быть ошибок `WebAppPopupParamInvalid`

3. **В Telegram:**
   - Нажмите /start
   - Должна появиться кнопка "🚀 Открыть Web App"
   - При нажатии откроется веб-интерфейс

---

## ❌ Частые ошибки

### Ошибка: `WebAppPopupParamInvalid`

**Причина**: URL не соответствует требованиям Telegram

**Решение:**
- Убедитесь, что URL начинается с `https://`
- Не используйте localhost или IP адреса
- Используйте Ngrok для локального тестирования

### Ошибка: `WEBAPP_URL не установлена`

**Причина**: Переменная не добавлена в .env

**Решение:**
```bash
# Создайте или отредактируйте .env файл
echo WEBAPP_URL=https://ваш-url.ngrok.io >> .env
```

### Web App не открывается

**Причина**: Сервер не запущен или недоступен

**Решение:**
1. Проверьте, что `main_with_webapp.py` запущен
2. Проверьте логи сервера
3. Проверьте доступность URL в браузере

---

## 🧪 Тестирование без Web App

Если Web App не настроен, бот полностью функционален через команды:

- `/start` - главное меню
- Все функции доступны через inline-кнопки
- WebApp - это дополнительный интерфейс, не обязательный для работы

---

## 📚 Дополнительные материалы

- [Telegram WebApp документация](https://core.telegram.org/bots/webapps)
- [Ngrok документация](https://ngrok.com/docs)
- [Amvera.io документация](https://docs.amvera.io)

---

**Обновлено**: 11 ноября 2025
