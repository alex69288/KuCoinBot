{
  "name": "kucoin-bot-monorepo",
  "version": "0.1.31",
  "private": true,
  "scripts": {
    "postinstall": "test -d backend/node_modules || (cd backend && npm install)",
    "build": "cd backend && npm run build",
    "start": "cd backend && node dist/index.js",
    "dev": "cd backend && npm run dev"
  }
}